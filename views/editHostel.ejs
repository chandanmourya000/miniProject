<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= hostel.hostelName %> - Details</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
    <style>
        :root {
            --primary-color: #4a6fa5;
            --secondary-color: #166088;
            --accent-color: #ff9d00;
            --background-color: #f8f9fb;
            --card-bg: #ffffff;
            --text-primary: #2d3748;
            --text-secondary: #4a5568;
            --text-light: #718096;
            --border-radius: 12px;
            --box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            color: var(--text-primary);
            background-color: var(--background-color);
            line-height: 1.7;
            padding: 0;
            margin: 0;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 2rem;
        }

        header {
            background-color: var(--primary-color);
            color: white;
            padding: 2rem 0;
            margin-bottom: 2rem;
            box-shadow: var(--box-shadow);
        }

        header h1 {
            margin: 0;
            padding: 0;
            border: none;
            font-size: 2.5rem;
            font-weight: 700;
            color: white;
        }

        .section {
            background-color: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: var(--box-shadow);
        }

        h2 {
            color: var(--secondary-color);
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 2px solid rgba(0, 0, 0, 0.05);
            display: flex;
            align-items: center;
        }

        h2 i, h2::before {
            margin-right: 0.75rem;
            color: var(--accent-color);
        }

        h3 {
            color: var(--secondary-color);
            font-size: 1.25rem;
            margin: 1.5rem 0 1rem;
            display: flex;
            align-items: center;
        }

        h3 i {
            margin-right: 0.5rem;
            color: var(--accent-color);
        }

        p {
            margin-bottom: 1rem;
            color: var(--text-secondary);
        }

        /* Form styling */
        .form-container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 0;
        }

        .form-header {
            margin-bottom: 1.5rem;
        }

        .form-footer {
            margin-top: 1.5rem;
            text-align: center;
            color: var(--text-light);
            font-style: italic;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--text-secondary);
        }

        .checkbox-group {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1rem;
            margin-top: 0.5rem;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .checkbox-item input[type="checkbox"] {
            margin-right: 0.5rem;
            cursor: pointer;
            accent-color: var(--accent-color);
            width: 18px;
            height: 18px;
        }

        .checkbox-item label {
            cursor: pointer;
            display: inline;
            margin: 0;
        }

        input[type="text"], 
        input[type="number"],
        input[type="email"],
        select,
        textarea {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            font-family: inherit;
            font-size: 1rem;
            color: var(--text-primary);
            transition: var(--transition);
        }

        input:focus, 
        select:focus,
        textarea:focus {
            outline: none;
            border-color: var(--accent-color);
            box-shadow: 0 0 0 2px rgba(255, 157, 0, 0.1);
        }

        textarea {
            resize: vertical;
            min-height: 120px;
        }

        select {
            background-color: white;
            cursor: pointer;
        }

        .section-title {
            font-size: 1.2rem;
            color: var(--secondary-color);
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
        }

        .section-title i {
            margin-right: 0.5rem;
            color: var(--accent-color);
        }

        button {
            background-color: var(--accent-color);
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            font-size: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        button i {
            margin-right: 0.5rem;
        }

        button:hover {
            background-color: #e68a00;
            transform: translateY(-2px);
        }

        .back-link {
            display: inline-block;
            margin-top: 1rem;
            margin-bottom: 2rem;
            padding: 0.75rem 1.5rem;
            background-color: var(--primary-color);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 500;
            transition: var(--transition);
        }

        .back-link:hover {
            background-color: var(--secondary-color);
            transform: translateY(-2px);
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }
            
            .section {
                padding: 1.5rem;
            }
            
            .checkbox-group {
                grid-template-columns: 1fr;
            }
            
            h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>Edit Hostel Details</h1>
        </div>
    </header>

    <div class="container">
        <div class="form-container">
            <section class="section">
                <h2><i class="fas fa-edit"></i> Edit Hostel Information</h2>
                <p>Modify the hostel information below</p>

                <form action="/admin/edit/<%= hostel._id %>" method="POST" class="form-body">
                    <div class="form-group">
                        <label for="hostelName">Hostel Name</label>
                        <input type="text" id="hostelName" name="hostelName" value="<%= hostel.hostelName %>" required>
                    </div>

                    <div class="form-group">
                        <label for="price">Price (per month)</label>
                        <input type="number" id="price" name="price" value="<%= hostel.price %>" required>
                    </div>

                    <div class="form-group">
                        <label for="category">Category</label>
                        <select name="category" id="category" required>
                            <option value="Boys Hostel" <%= hostel.category === "Boys Hostel" ? "selected" : "" %>>Boys Hostel</option>
                            <option value="Girls Hostel" <%= hostel.category === "Girls Hostel" ? "selected" : "" %>>Girls Hostel</option>
                            <option value="Apartment" <%= hostel.category === "Apartment" ? "selected" : "" %>>Apartment</option>
                            <option value="PG" <%= hostel.category === "PG" ? "selected" : "" %>>PG</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="contact">Hostel Contact Number</label>
                        <input type="text" name="contact" id="contact" value="<%= hostel.contact %>" required>
                    </div>

                    <div class="form-group">
                        <h3><i class="fas fa-bed"></i> Room Types Available</h3>
                        <div class="checkbox-group">
                            <div class="checkbox-item">
                                <input type="checkbox" id="single" name="sharing[]" value="Single" <%= hostel.sharing.includes("Single") ? "checked" : "" %>>
                                <label for="single">Single Room</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="double" name="sharing[]" value="Double" <%= hostel.sharing.includes("Double") ? "checked" : "" %>>
                                <label for="double">Double Sharing</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="triple" name="sharing[]" value="Triple" <%= hostel.sharing.includes("Triple") ? "checked" : "" %>>
                                <label for="triple">Triple Sharing</label>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="address">Address</label>
                        <textarea id="address" name="address" required><%= hostel.address %></textarea>
                    </div>

                    <div class="form-group">
                        <label for="description">About the Hostel</label>
                        <textarea id="description" name="description" required><%= hostel.description %></textarea>
                    </div>

                    <div class="form-group">
                        <h3><i class="fas fa-concierge-bell"></i> Facilities & Amenities</h3>
                        <div class="checkbox-group">
                            <div class="checkbox-item">
                                <input type="checkbox" id="wifi" name="facilities[]" value="WiFi" <%= hostel.facilities.includes("WiFi") ? "checked" : "" %>>
                                <label for="wifi">WiFi</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="mess" name="facilities[]" value="Mess" <%= hostel.facilities.includes("Mess") ? "checked" : "" %>>
                                <label for="mess">Mess</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="geyser" name="facilities[]" value="Geyser" <%= hostel.facilities.includes("Geyser") ? "checked" : "" %>>
                                <label for="geyser">Geyser</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="waterpurifier" name="facilities[]" value="Water Purifier" <%= hostel.facilities.includes("Water Purifier") ? "checked" : "" %>>
                                <label for="waterpurifier">Water Purifier</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="washingmachine" name="facilities[]" value="Washing Machine" <%= hostel.facilities.includes("Washing Machine") ? "checked" : "" %>>
                                <label for="washingmachine">Washing Machine</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="ac" name="facilities[]" value="Air Conditioner" <%= hostel.facilities.includes("Air Conditioner") ? "checked" : "" %>>
                                <label for="ac">Air Conditioner</label>
                            </div>
                        </div>
                    </div>

                    <button type="submit"><i class="fas fa-check-circle"></i> Update Hostel</button>
                </form>
            </section>

            <a href="javascript:history.back()" class="back-link"><i class="fas fa-arrow-left"></i> Back to Hostels</a>

            <div class="form-footer">
                Make sure the edited details are accurate before saving.
            </div>
        </div>
    </div>
</body>
</html>
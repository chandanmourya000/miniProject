<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Users - Accommodation Portal</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/styleMain.css">
    <style>
        :root {
            --primary-gradient: linear-gradient(to right, #990000, #0073e6);
            --section-bg: #ffffff;
            --text-primary: #333;
            --text-secondary: #555;
            --border-radius: 10px;
            --box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            --accent-color: #0073e6;
            --secondary-accent: #990000;
            --success-color: #28a745;
            --danger-color: #dc3545;
            --warning-color: #ffc107;
            --info-color: #17a2b8;
            --dark-color: #343a40;
            --card-hover-transform: translateY(-5px);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            color: var(--text-primary);
            background-color: #f5f7fa;
            line-height: 1.6;
            padding: 0;
            margin: 0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* Page Header - Styled like admin dashboard */
        .page-header {
            background: var(--primary-gradient);
            color: white;
            padding: 40px;
            margin-bottom: 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            position: relative;
            overflow: hidden;
        }

        .page-header h1 {
            font-size: 32px;
            margin-bottom: 10px;
            font-weight: 600;
            position: relative;
            z-index: 1;
        }

        .page-header p {
            font-size: 18px;
            opacity: 0.9;
            max-width: 80%;
            color: white;
            position: relative;
            z-index: 1;
        }

        .page-header::after {
            content: '';
            position: absolute;
            top: -50px;
            right: -50px;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
        }

        /* User Stats Dashboard */
        .user-stats {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }
        
        .stat-card {
            background: var(--section-bg);
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            padding: 20px;
            flex: 1;
            min-width: 200px;
            margin: 0 10px 20px 10px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .stat-card:hover {
            transform: var(--card-hover-transform);
            box-shadow: 0 12px 20px rgba(0, 0, 0, 0.15);
        }
        
        .stat-card .stat-number {
            font-size: 36px;
            font-weight: bold;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            margin: 10px 0;
        }
        
        .stat-card .stat-icon {
            font-size: 36px;
            margin: 10px 0;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .stat-card .stat-label {
            font-size: 16px;
            color: var(--text-secondary);
        }

        .section {
            background-color: var(--section-bg);
            border-radius: var(--border-radius);
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: var(--box-shadow);
        }

        .section-title {
            color: var(--text-primary);
            font-size: 1.5rem;
            margin-top: 0;
            margin-bottom: 20px;
            position: relative;
            padding-bottom: 10px;
            border-bottom: 2px solid rgba(0, 115, 230, 0.2);
            display: flex;
            align-items: center;
        }
        
        .section-title::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: -2px;
            width: 60px;
            height: 2px;
            background: var(--primary-gradient);
        }

        .section-title i {
            margin-right: 12px;
            color: var(--accent-color);
        }

        .empty-state {
            text-align: center;
            padding: 50px 20px;
            color: var(--text-secondary);
        }

        .empty-state i {
            font-size: 5rem;
            margin-bottom: 20px;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .empty-state p {
            font-size: 1.2rem;
            margin: 0;
            opacity: 0.8;
        }

        .users-list {
            list-style: none;
            padding: 0;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
        }

        .user-card {
            background-color: var(--section-bg);
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            overflow: hidden;
            transition: all 0.3s ease;
            border: 1px solid rgba(0, 0, 0, 0.05);
            position: relative;
        }

        .user-card:hover {
            transform: var(--card-hover-transform);
            box-shadow: 0 12px 20px rgba(0, 0, 0, 0.15);
        }

        .user-card::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 4px;
            background: var(--primary-gradient);
        }

        .user-header {
            background: var(--primary-gradient);
            color: white;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .user-name {
            font-size: 1.3rem;
            font-weight: 600;
            margin: 0;
        }

        .user-email {
            font-size: 0.9rem;
            opacity: 0.9;
        }

        .user-body {
            padding: 20px;
        }

        .user-info {
            margin-bottom: 20px;
        }

        .info-item {
            display: flex;
            margin-bottom: 12px;
            padding-bottom: 12px;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
        }

        .info-item:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .info-label {
            font-weight: 600;
            color: var(--text-secondary);
            width: 100px;
            flex-shrink: 0;
            display: flex;
            align-items: center;
        }

        .info-label i {
            margin-right: 8px;
            color: var(--accent-color);
            width: 16px;
        }

        .info-value {
            color: var(--text-primary);
            font-weight: 500;
        }

        .preferences-toggle {
            background-color: #f8f9fa;
            border: none;
            border-radius: var(--border-radius);
            padding: 12px 16px;
            width: 100%;
            text-align: left;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-weight: 600;
            color: var(--accent-color);
            margin-bottom: 15px;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
        }

        .preferences-toggle:hover {
            background-color: #edf2f7;
            transform: translateY(-2px);
        }

        .preferences-toggle:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(0, 115, 230, 0.2);
        }

        .preferences-toggle i {
            transition: all 0.3s ease;
        }

        .preferences-toggle[aria-expanded="true"] i {
            transform: rotate(180deg);
        }

        .preferences-content {
            background-color: #f8f9fa;
            border-radius: var(--border-radius);
            padding: 16px;
            margin-bottom: 15px;
            display: none;
        }

        .preferences-content.visible {
            display: block;
            animation: fadeIn 0.3s ease-out forwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .actions {
            display: flex;
            justify-content: flex-end;
            margin-top: 20px;
        }

        .delete-btn {
            background-color: var(--danger-color);
            color: white;
            border: none;
            border-radius: var(--border-radius);
            padding: 10px 16px;
            cursor: pointer;
            font-weight: 500;
            display: flex;
            align-items: center;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
            box-shadow: 0 4px 10px rgba(220, 53, 69, 0.2);
        }

        .delete-btn i {
            margin-right: 8px;
        }

        .delete-btn:hover {
            background-color: #c82333;
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(220, 53, 69, 0.3);
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            margin-top: 10px;
            margin-bottom: 30px;
            padding: 12px 24px;
            background: var(--primary-gradient);
            color: white;
            text-decoration: none;
            border-radius: var(--border-radius);
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: var(--box-shadow);
        }

        .back-link:hover {
            transform: var(--card-hover-transform);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }

        .back-link i {
            margin-right: 10px;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }
            
            .users-list {
                grid-template-columns: 1fr;
            }
            
            .user-header {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .user-email {
                margin-top: 0.25rem;
            }
            
            .page-header {
                padding: 30px 20px;
            }
            
            .page-header p {
                max-width: 100%;
            }
            
            .stat-card {
                margin: 0 0 15px 0;
                min-width: 100%;
            }
        }
    </style>
</head>
<body>
    <header>
        <div id="acc1st"> 
            <div id="logoimg">
                <img src="/logo.jpg" alt="logo">
            </div>
            <h1 id="accLetter">Accommodation</h1>
        </div> 
            
        <div class="navbar">
            <div onclick="location.href='/admindashboard'">Home</div>
            <div><a href="#abc">About</a></div>
            <div>
                <a href="https://hostels.cusat.ac.in/">College Hostel</a>
            </div>
            <!-- private hostel -->
            <div class="dropdown">
                <button class="dropbtn">Private Hostel</button>
                <div class="dropdown-content">
                    <div class="dropdown-item" onclick="location.href='/apartment'">Apartment</div>
                    <div class="dropdown-item" onclick="location.href='/pg'">PG</div>
                    <div class="dropdown-item" onclick="location.href='/boyshostel'">Boys Hostel</div>
                    <div class="dropdown-item" onclick="location.href='/girlshostel'">Girls Hostel</div>
                </div>
            </div>
            <div><a href="/matchroommates">🔍 Find a Roommate</a></div>
        </div>
    </header>

    <div class="container">
        <!-- Welcome Section styled like admin dashboard -->
        <section class="page-header">
            <h1>User Management</h1>
            <p>Manage user accounts and monitor platform activity from this centralized dashboard.</p>
        </section>

        <!-- User Stats -->
        <div class="user-stats">
            <div class="stat-card">
                <div class="stat-number"><%= users.length %></div>
                <div class="stat-label">Registered Users</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon"><i class="fas fa-users"></i></div>
                <div class="stat-label">User Management</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon"><i class="fas fa-user-shield"></i></div>
                <div class="stat-label">Admin Tools</div>
            </div>
        </div>

        <section class="section">
            <h2 class="section-title"><i class="fas fa-users"></i> Registered Users</h2>

            <% if (users.length === 0) { %>
                <div class="empty-state">
                    <i class="fas fa-user-slash"></i>
                    <p>No users found in the system.</p>
                </div>
            <% } else { %>
                <ul class="users-list">
                    <% users.forEach(function(user) { %>
                        <li class="user-card">
                            <div class="user-header">
                                <div>
                                    <h3 class="user-name"><%= user.username %></h3>
                                    <div class="user-email"><%= user.email %></div>
                                </div>
                                <i class="fas fa-user-circle fa-lg"></i>
                            </div>
                            
                            <div class="user-body">
                                <div class="user-info">
                                    <div class="info-item">
                                        <div class="info-label"><i class="fas fa-venus-mars"></i> Gender:</div>
                                        <div class="info-value"><%= user.gender || "Not specified" %></div>
                                    </div>
                                    <div class="info-item">
                                        <div class="info-label"><i class="fas fa-phone"></i> Contact:</div>
                                        <div class="info-value"><%= user.contactNumber || "Not provided" %></div>
                                    </div>
                                </div>
<!--                                 
                                <% if (user.preferences) { %>
                                    <button class="preferences-toggle" aria-expanded="false" onclick="togglePreferences(this)">
                                        <span>User Preferences</span>
                                        <i class="fas fa-chevron-down"></i>
                                    </button>
                                    
                                    <div class="preferences-content" id="pref-<%= user._id %>">
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-sun"></i> Wake Up:</div>
                                            <div class="info-value"><%= user.preferences.wakeUpTime %></div>
                                        </div>
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-moon"></i> Sleep:</div>
                                            <div class="info-value"><%= user.preferences.sleepTime %></div>
                                        </div>
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-smoking"></i> Smoking:</div>
                                            <div class="info-value"><%= user.preferences.smoking ? "Yes" : "No" %></div>
                                        </div>
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-glass-cheers"></i> Drinking:</div>
                                            <div class="info-value"><%= user.preferences.drinking ? "Yes" : "No" %></div>
                                        </div>
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-broom"></i> Cleanliness:</div>
                                            <div class="info-value"><%= user.preferences.cleanliness %></div>
                                        </div>
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-walking"></i> Lifestyle:</div>
                                            <div class="info-value"><%= user.preferences.lifestyle %></div>
                                        </div>
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-utensils"></i> Diet:</div>
                                            <div class="info-value"><%= user.preferences.diet %></div>
                                        </div>
                                        <div class="info-item">
                                            <div class="info-label"><i class="fas fa-laugh"></i> Personality:</div>
                                            <div class="info-value"><%= user.preferences.personality %></div>
                                        </div>
                                    </div>
                                <% } %> -->
                                
                                <div class="actions">
                                    <form action="/admin/users/delete/<%= user._id %>" method="POST">
                                        <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this user? This action cannot be undone.')">
                                            <i class="fas fa-trash-alt"></i> Delete User
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </li>
                    <% }) %>
                </ul>
            <% } %>
        </section>

        <a href="/admindashboard" class="back-link"><i class="fas fa-arrow-left"></i> Back to Dashboard</a>
    </div>

    <script>
        function togglePreferences(element) {
            // Toggle the expanded state
            const expanded = element.getAttribute('aria-expanded') === 'true';
            element.setAttribute('aria-expanded', !expanded);
            
            // Get the next sibling element (preferences content)
            const content = element.nextElementSibling;
            content.classList.toggle('visible');
        }
    </script>
</body>
</html>